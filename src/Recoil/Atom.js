import {atom} from 'recoil'

export const imageAdress = atom({
    key:'adress',
    default:[]
})

export const font = atom({
    key:'size',
    default:[]
})
export const titleAtom = atom({
    key:'title',
    default:'Untitle document'
})